# CLAUDE.md — project memory

<!--
  Claude Code auto-loads this file as memory whenever it works in this project.
  Keep it short and high-signal (it's added to context on every request).
  Replace every <placeholder> and delete what you don't need.
  Full, commented template: the Claude Code Power Pack.
-->

## Project

<one or two sentences: what this project is>

- **Stack:** <languages, frameworks, key libraries>
- **Entry points:** <where execution starts>

## Commands

- Install: `<install command>`
- Run / dev: `<run command>`
- Test: `<test command>`
- Lint / format: `<lint command>`

## Conventions

- Keep changes small and scoped; don't refactor untouched code.
- Match the style of the surrounding file.
- Never commit secrets or `.env*` files.
- <any project-specific rule>

## Do not touch

- <generated / vendored paths, e.g. dist/, build/>
- <anything that needs special care>
